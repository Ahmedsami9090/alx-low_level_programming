# include "main.h"

/**
 * reverse_array - entry
 *
 * @a: string array
 *
 * @n: no of elements
 *
 * Return: reversed array
*/

void reverse_array(int *a, int n)
{
	int i;

	while (i != 0)
	{
		_putchar(',');
		_putchar(' ');
	}
	for (i = n - 1; i >= 0; i--)
	{
		a[i];
	}
}
